<Window
    x:Class="yoksdotnet.windows.PaletteExportDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:ydnwindows="clr-namespace:yoksdotnet.windows"
    mc:Ignorable="d"
    Title="Export palette set" 
    Width="400"
    Height="300"
>
    <Window.DataContext>
        <ydnwindows:PaletteExportViewModel x:Name="ViewModel" />
    </Window.DataContext>
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="5,2" />
            <Setter Property="Margin" Value="5,2" />
        </Style>
    </Window.Resources>

    <DockPanel Margin="8">
        <Label DockPanel.Dock="Top">Copy the text below to share this palette set with a friend.</Label>
        <DockPanel DockPanel.Dock="Bottom" LastChildFill="False">
            <Button DockPanel.Dock="Right" Click="OnClose">Close</Button>
        </DockPanel>
        
        <TextBox 
			Padding="4" 
			FontFamily="Consolas" 
			TextWrapping="Wrap" 
			IsReadOnly="True" 
			Text="{Binding ExportCode}" 
	    />
    </DockPanel>
</Window>